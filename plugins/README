Place here the Plugins you want to add to RosarioSIS.

Activate them via:
School Setup > School Configuration > Plugins

Every plugin must have a functions.php file. The functions.php file contains the functions to be automatically loaded by RosarioSIS.
