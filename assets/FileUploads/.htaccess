## Begin - Security
# Block access to web-executable files inside the uploads folder
<IfModule mod_rewrite.c>

    RewriteEngine On

    RewriteRule (?i)\.(php.?|phar|[psx]?html?|xml|svg|js|pl|py|cgi|sh|bat)$ - [F]
</IfModule>
#
## End - Security
